{
  "buildCommand": "cd frontend && bun install && bun run build && node ../scripts/postbuild.js",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
